# ClipSeek full dependency set.
#
# Recommended on Windows: double-click install_clipseek.bat. It installs into
# the user's active Python environment with pip --user and does not create a venv.
-r requirements-clipseek.txt
