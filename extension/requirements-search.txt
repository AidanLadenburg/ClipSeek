# Python dependencies for io.py / io.exe (Cosmos-Embed1 search backend)
# faiss-cpu: optional but recommended — enables "FAISS" search mode in the panel (approximate ANN + GPU rerank).
torch
transformers>=4.46
numpy
decord
Pillow
einops
faiss-cpu
